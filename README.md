# routemap-media

Public hosting for RouteMap social media assets. Files here are referenced by
URL from Buffer posts, so nothing may be deleted or renamed while a post that
uses it is still scheduled. Written by `npm run ingest` in routemap-social.
